<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.andrespiraquive.recettes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icone_recipes"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icone_recipes"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".GridViewActivity"
            android:label="@string/app_name"></activity>

        <activity
            android:name=".Views.MainActivity"
            android:label="@string/title_user_settings"></activity>

        <activity
            android:name=".RegisterActivity"
            android:label="@string/app_name"></activity>

        <activity
            android:name=".AddRecipeActivity"
            android:label="@string/title_add_recipe"></activity>

        <activity
            android:name=".SearchActivity"
            android:label="@string/title_search_recipe"></activity>

        <activity
            android:name=".RecipeActivity"
            android:label="@string/title_recipe"></activity>

        <activity android:name=".favorisActivity"
            android:label="Favoris" />
    </application>

</manifest>